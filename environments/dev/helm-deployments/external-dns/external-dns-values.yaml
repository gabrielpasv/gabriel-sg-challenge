replicaCount: 1

provider: aws

aws:
  region: us-east-1
  zoneType: public

policy: sync

domainFilters:
  - gpvicente.com

txtOwnerId: dev-externaldns 

rbac:
  create: true

serviceAccount:
  create: true
  annotations:
    eks.amazonaws.com/role-arn:

logLevel: info
